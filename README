Installation:

    git clone git://github.com/RoninZero/dotvim.git ~/.vim

Create symlinks
	
    ln -s ~/.vim/vimrc ~/.vimrc
    ln -s ~/.vim/gvimrc ~/.gvimrc

Install additional pre-req packages
    apt-get install build-essential cmake python-dev python3-dev

Update (install plugins)
    open "vim"
    run ":PlugInstall"

